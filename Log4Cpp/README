To build the C++ logging library Log4Cpp you will probably need
to apply a patch if you're using a recent C++. To Build

  tar -zxf log4cpp-1.0.tar.gz
  patch -p0 < log4cpp-1.0.patch
  cd log4cpp-1.0
  ./configure --prefix=$MA
  make -j4
  make install

